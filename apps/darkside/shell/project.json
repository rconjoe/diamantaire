{
  "name": "darkside-shell",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/darkside/shell",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/darkside/shell",
        "outputPath": "dist/apps/darkside/shell"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/darkside/shell"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "darkside-shell:build",
        "dev": true,
        "proxyConfig": "apps/darkside/shell/proxy.conf.json"
      },
      "configurations": {
        "development": {
          "buildTarget": "darkside-shell:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "darkside-shell:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "darkside-shell:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/darkside/shell/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/darkside/shell/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
