#!/usr/bin/env bash

set -euo pipefail

. prelude

upgrade_node() {
  log_debug "Attempting node upgrade"
  gum confirm "can i use nvm to upgrade node?" && gum spin -s points --title "installing node v20.8.1..." -- "$(sleep 5)"
}

export -f upgrade_node
