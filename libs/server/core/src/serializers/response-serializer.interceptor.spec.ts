import { ResponseSerializerInterceptor } from './response-serializer.interceptor';

describe('ResponseSerializerInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseSerializerInterceptor()).toBeDefined();
  });
});
