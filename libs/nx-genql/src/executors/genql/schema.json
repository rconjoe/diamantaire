{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Genql executor",
  "description": "",
  "type": "object",
  "properties": {
    "endpoint": {
      "type": "string",
      "description": "The graphql endpoint to use"
    },
    "token": {
      "type": "string",
      "description": "The token to use for authentication"
    },
    "scalarTypes": {
      "type": "object",
      "description": "Map of scalar types to use in the schema",
      "additionalProperties": {
        "type": "string"
      }
    },
    "output": {
      "type": "string",
      "description": "The output directory to write to"
    }
  },
  "required": []
}
