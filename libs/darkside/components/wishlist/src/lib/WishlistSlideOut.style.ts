import styled from 'styled-components';

export const StyledWishlistSlideOut = styled.div`
  .slideout__title {
    text-align: center;
    font-size: var(--font-size-medium);
    margin-bottom: 1rem;
  }
`;
