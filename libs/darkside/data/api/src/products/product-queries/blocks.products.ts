// For BlockPicker components that need products, we need to use the following query:

export async function fetchProductByVariantSlugs(locale: string, slugs: string[]) {
  console.log(locale, slugs);

  //   const apiUrl = `/v1/products?${searchParams.toString()}`;

  //   const response = await vraiApiClient.get(apiUrl);

  //   return response.data;
}
